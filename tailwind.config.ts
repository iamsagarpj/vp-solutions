// Tailwind v4 — config is CSS-driven via @theme in globals.css.
// This file is kept for tooling compatibility only.
import type { Config } from "tailwindcss";

const config: Config = {
  content: ["./src/**/*.{ts,tsx}"],
};

export default config;
